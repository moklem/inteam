{
  "name": "volleyball-app-server",
  "version": "1.0.0",
  "description": "Backend server for volleyball club management app",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "seed": "node scripts/seedData.js",
    "build": "echo 'No build required for backend'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.1",
    "mongoose": "^7.5.0",
    "morgan": "^1.10.0",
    "volleyball-app": "file:.."
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "path-browserify": "^1.0.1"
  }
}
